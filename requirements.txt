# Install PyTorch separately — see setup_gpu.sh or README for CUDA wheels.
# Base deps (CPU or GPU venv):
timm>=0.9.0
numpy>=1.24.0
opencv-python>=4.8.0
einops>=0.7.0
scikit-learn>=1.3.0
PyYAML>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0

# Optional: ball/player detection (detect_ball_players.py)
ultralytics>=8.0.0
